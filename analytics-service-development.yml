spring:
  application:
    name: config-client
  profiles:
    active: development
  config:
    import: "optional:configserver:http://localhost:8888"

analytics:
  greeting: Hey analytics GIT
server:
  port: 4002



